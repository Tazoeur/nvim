## TODO
* add description to leader r (reload nvim configuration)
* tab spaces in DBUI should be reduced
* tab spaces in SQL should be consistant with indent space
* schemas in dbui should be ordered by name
* dadbod `select count(*) from x` results in a folded results in dbui, how to prevent that ?
* add illuminate plugin (configuration is commented from a lazy installer)

## Questions

